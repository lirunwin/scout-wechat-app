<!--template/sms/sms.wxml-->
<template name="smsBasic">
  <view>
    <input name="tel" type='digit' placeholder="请输入手机号码" bindinput='bindTelInput' />
    <view class='smsBasic'>
      <input name="sms" type='digit' placeholder="请输入验证码" bindinput='bindSmsInput'/>
      <view>
        <button class='btn btn-outline-primary {disableSmsBtn? "disabled": ""}}' 
          disabled='{{disableSmsBtn}}'
          bindtap='getSmsCode'>获取验证码{{smsMsg}}</button>
      </view>
    </view>
  </view>
</template>